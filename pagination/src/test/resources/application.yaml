spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    jdbcUrl: jdbc:h2:mem:datasource
    driver-class-name: org.h2.Driver
    username: root
    password: root